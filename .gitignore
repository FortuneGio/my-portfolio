node_modules
.next
out
.env*.local
*.tsbuildinfo
next-env.d.ts

# Local build/debug logs — never published.
*.log

# Dev-only tooling: capture, audit and media-staging scripts. They reference
# paths in the handoff root, which is not part of this repository.
/scripts/

# Regenerated by `next dev` on every run.
/AGENTS.md
/CLAUDE.md
